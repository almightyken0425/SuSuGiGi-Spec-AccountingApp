# 搜尋: SearchScreen

## 畫面目標

- 提供搜尋所有交易與轉帳備註的介面，並作為導航至紀錄編輯畫面的入口

---

## 觸發情境

- 以 Modal 呈現
- 由點擊首頁搜尋入口觸發

---

## 線框圖

```text
+----------------------------------+
| [X]         搜尋                 |
+----------------------------------+
| .------------------------------. |
| | [Icon] Category     Amount   | |
| |        Note(hl)         Date | |
| |------------------------------| |
| | [Icon] Category  [⟳] Amount  | |
| |        Note(hl)         Date | |
| '------------------------------' |
+----------------------------------+
| [ Search...                    ] |
+----------------------------------+
```

---

## 佈局

### 導覽列

- Header 模式: D
- 關閉按鈕
- 搜尋 標題

### 搜尋輸入區

- Search 模式: Bottom Pill

### 搜尋結果區

- List 模式: Custom
- 含關鍵字 highlight、雙列佈局
- 視覺對齊對側 Design git 的 TX_LIST_TOKENS 群組
- **IF** 搜尋框為空:
  - 顯示 "輸入關鍵字以搜尋交易" 提示
- **IF** 無符合結果:
  - 顯示 "找不到結果" 提示
  - 副標回顯當前搜尋關鍵字
- 搜尋結果列表
  - 外層由群組卡片包覆
  - row 間以 hairline 分隔線分隔
  - 交易與轉帳各自最多顯示 50 筆
  - 依日期由新至舊排序
- **單筆紀錄列:**
  - 雙列佈局
  - **上列:**
    - 左：類別圖示置於 outline 框內
    - 中：類別名稱或轉帳標籤
    - 右：金額
  - **下列:**
    - 左：備註，關鍵字以主色高亮
    - 右：日期
  - 金額顯色：支出採錯誤色、收入採成功色、零值採主文字色
  - **IF** 該筆為定期排程實例:
    - 金額左側並列循環圖示
    - 圖示使用停用態前景色，視覺權重低於金額
    - 圖示本身不獨立觸發互動，點按列的行為與一般紀錄相同

---

## 互動

- **點按關閉按鈕:**
  - 關閉 Modal

- **輸入搜尋文字:**
  - 輸入後 debounce 自動觸發搜尋，更新搜尋結果列表

- **返回此畫面時:**
  - **IF** 搜尋框非空:
    - 重新執行搜尋，反映編輯畫面可能造成的資料變動

- **點按搜尋結果列表項目:**
  - **IF** 交易紀錄:
    - 導航至 TransactionEditorScreen
  - **IF** 轉帳紀錄:
    - 導航至 TransferEditorScreen
